// import colors from './colors'

// const makeSetPixel = pixels => {
//   const setPixel = (x, y, c = 0) => {
//     if (x < 0 || x >= 128 || y < 0 || y >= 128) return
//     const int = colors.int(c)
//     if (int) {
//       const newColor = int
//       pixels[y * 128 + x] = newColor
//     }
//   }
//   return setPixel
// }

// export default makeSetPixel
