/* eslint no-restricted-globals: 1 */
self.setPixel = (x, y, c = 0) => {
  // if (x < 0 || x >= 128 || y < 0 || y >= 128) return
  // const int = self.colors.int(c)
  // if (int) {
  //   const newColor = int
  //   const index = y * 128 + x
  //   self.pixels[index] = newColor
  // }
}
